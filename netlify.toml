[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Sitio: https://bertacaceresrepresentare.netlify.app/
# SPA routing: public/_redirects se copia a dist/ durante el build de Vite.
#
# Tokens Mapbox: Vite lee .env.production en el build (VITE_MAPBOX_TOKEN, etc.).
# Alternativa: definir las mismas variables en Netlify → Environment variables.
